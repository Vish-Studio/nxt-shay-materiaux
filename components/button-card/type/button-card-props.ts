export default interface ButtonCardProps {
  className?: string;
  title: string;
  iconName: string;
  numTotal: string;
  numTotalTxt: string;
  redirect: string;
  fabRedirect: string;
  dataLoaded?: boolean;
  showContent?: boolean;
}
